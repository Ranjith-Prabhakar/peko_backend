const signupController = require("./signUp.controller");
const loginController = require("./login.controller");
const refreshController = require("./refresh.controller");
const logutController = require("./logout.controller");

module.exports = {
  signupController,
  loginController,
  refreshController,
  logutController,
};
